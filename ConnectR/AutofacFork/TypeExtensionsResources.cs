namespace MediatR.ConnectR
{
    public static class TypeExtensionsResources
    {
        public static string NotOpenGenericType
            => "The type '{0}' is not an open generic class or interface type so it won't work with methods that act on open generics.";
    }
}
